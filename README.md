# Segeran Jiwa POS Next

Workspace project: **Segeran Jiwa Next Vol. 1**.

Current implementation milestone: **CS-01 — Core System: Repository, Mobile Inbox & Automation Foundation**.

Blueprint authority: **v1.0 FINAL LOCK**. This repository is new and does not import the legacy Segeran Jiwa POS source.

## Runtime

- Node.js 24 (`.nvmrc`)
- npm with committed `package-lock.json` after dependency bootstrap
- React + TypeScript + Vite

## Verification

The canonical command will be:

```bash
npm run verify
```

During Task 1 bootstrap, install dependencies with the approved CS-01 R2 commands in a Node 24 environment before the bootstrap commit is allowed.
